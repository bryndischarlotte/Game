// Test file 
const Player = require("./player");
// Player tests
    test('Should return player as X', () => {
        const testPlayer = new Player('X');
        expect(testPlayer.symbol).toBe('X');
    });

    test('changePlayer should increment totalMoves by 1', () => {
        expect(functions.changePlayer.toEqual({
            let counter = 0;

        });
    });

